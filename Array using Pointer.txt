#include <stdio.h>

int main()
{
    int arr[5] = {10, 20, 30, 40, 50};
    int length = sizeof(arr) / sizeof(arr[0]);
    int *p = arr;

    for (int i = 0; i < length; i++)
    {
        printf("Index %d = %d\n", i, *p);
        p++;
    }

    return 0;
}